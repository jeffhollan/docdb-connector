{
  "name": "docdb-connector",
  "version": "0.1.0",
  "description": "Logic App connector for Azure DocumentDB",
  "main": "app.js",
  "scripts": {
    "test": "mocha",
    "start": "node app.js",
    "build": "tsc",
    "prepublish": "typings install && npm run build",
    "postinstall": "tsc"
  },
  "author": "Jeff Hollan",
  "license": "MIT",
  "dependencies": {
    "restify": "^4.1.1"
  },
  "devDependencies": {
    "mocha": "^3.0.1",
    "nock": "^8.0.0",
    "should": "^10.0.0",
    "supertest": "^2.0.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  },
  "engines": {
    "node": "^6.3.0"
  }
}
